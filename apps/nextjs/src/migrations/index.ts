// Payload migrations are intentionally empty.
// Schema is managed by Drizzle-kit (src/server/db/migrations/).
// Payload's `push: false` config + empty migrations = Payload won't touch the schema.
export const migrations = [];
